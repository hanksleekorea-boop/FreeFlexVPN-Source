# 기여 방법

1. 기본 작업 갈래에 직접 올리지 말고 `feature/` 또는 `handoff/` 작업 갈래에서 작업합니다.
2. 수정 전후에 관련 검사와 전체 재검사(회귀) 결과를 기록합니다.
3. 비밀값, 실제 개인 정보, 실제 IP 주소는 어떤 파일에도 넣지 않습니다.
4. Pull Request에는 사용자에게 보이는 변화, 검사 결과, 되돌리는 방법을 적습니다.
