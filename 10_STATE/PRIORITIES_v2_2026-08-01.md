# FreeFlexVPN v2 우선순위

> 정본: `APP_SERVICE_PLAN_v2.0_2026-08-01.md`  
> 제품 기준선: `PLAN_v2_2026-08-01.md`  
> 상세 실행: `DEV_EXECUTION_PLAN_v2.0_2026-08-01.md`  
> 현재 v2 진척: 7/12 = 58.3%

## 0. v2.1 핵심 피처 반영 완료 (2026-08-02)

- VPN 사용 순간 Best 30과 `현재 국가 → 목적 → 목적 국가·본국 → 무료/유료 충전/프리미엄 예정` 추천을 공개했다.
- 현재 국가·목적·검색어는 메모리 전용이며 API로 보내거나 저장하지 않는다.
- 실제 health 서버 카탈로그에 존재하는 목적지만 행동 가능하다. 서버 0대 공개본에서는 추천 설명만 보이고 연결은 비활성이다.
- 구독료 국가 우회·BBC iPlayer 국외 우회는 비지원, 중국 등 검열 대응 미검증 환경은 전문 서비스 필요로 안내한다.
- 공개 후보 v2.1: PR #5 / main `33813cd` / 전체 회귀 343/343 / 공개 무캐시 3회 동일 해시.

이 릴리스는 중요한 사용자 가치가 공개된 진척이지만 실제 서버·실기기 관문을 대체하지 않으므로 전체 진척은 58.3%로 유지한다.

### v2.2 크로스플랫폼 반영

- Windows·macOS·Linux·Android·iOS를 자동 감지하고 수동 선택할 수 있는 `모든 기기` 화면을 추가했다.
- FreeFlexVPN PWA와 공식 WireGuard 터널의 역할을 분리했다.
- 실제 서버·로그인 전에는 기기 구성 발급을 비활성으로 유지한다.
- 다음 실제 관문에 Windows·macOS·Linux·Android·iOS별 설치·구성·보호 확인 행렬을 포함한다.

## 1. 지금 가장 먼저 만들 것

1. **서로 다른 공급자 2곳에 준비된 SSH 어댑터 주입** — 계정 수령 즉시 실제 host·known_hosts·서버 공개키를 프로젝트 밖 런타임 설정으로 넣고 `run_r6_server_preflight.py --config-only` 실행 후 실서버 모드 실행. 설정 준비와 실서버 PASS를 분리하고, 2공급자·최신 health·카운터·카탈로그가 한 번에 PASS해야 다음 단계로 이동
2. **두 노드 R6 실행** — WireGuard 피어 생성·폐기·카운터·출구 IP·장애 전환과 D4(한국 exit 금지) 확인
3. **목표 OS 안전 행렬 실행** — 같은 후보로 iOS·Android·Windows의 8개 안전 항목을 실제 기기에서 전건 확인
4. **공개 PWA에 실제 API 주소 주입** — API 소비·브라우저 키 생성·기기·지갑·추천·보호 UI는 구현 완료. 실제 HTTPS API와 동일 후보 검증 뒤 빈 meta를 정본 주소로 교체

제어 API·세션 계측·OS 안전 계약, strict SSH 실서버 어댑터, PWA API 소비·브라우저 X25519 키 생성은 로컬에서 완료했다. 공개 화면은 API 주소가 비어 있으면 네트워크 요청을 만들지 않고 지갑·추천을 예시·비활성 상태로 유지한다.

2026-08-02 추가 준비: R6 실서버 사전점검 계약·CLI와 누락/오래됨/미래 health fail-closed 방어를 구현했다. 이는 구현·로컬 증거이며 서버 0대 상태에서는 실행 PASS나 M8 완료를 주장하지 않는다.

v2.5에서는 동일 CLI에 네트워크 요청 0건의 `--config-only` 단계를 추가했다. 설정이 통과해도 `ready=false`를 고정해 실제 서버 증거와 섞이지 않는다.

v2.6에서는 실서버 판정의 공개 카탈로그를 현재 후보에 정확히 결속했다. 후보 밖 서버·중복 서버·빈 행이 하나라도 섞이면 fail-closed로 멈춘다. 연결 회귀 37/37과 검사기 사전진단 2/2를 통과했지만, 이는 I/L 증거이며 서버 0대·실기기 0건 경계와 7/12 = 58.3% 기준선은 유지한다.

v2.7에서는 실서버 모드가 `--config-evidence`를 필수로 받고, 직전 CONFIG READY 증거의 candidate ID와 설정 SHA-256이 현재 실행과 정확히 같을 때만 SSH를 시도한다. 후보·설정 불일치, 준비 미완료, 네트워크 요청 이력이 있는 설정 증거, 과대·누락 파일은 모두 서버 접속 전에 차단한다. 연결 회귀 42/42 통과, 전체 기준선은 7/12 = 58.3%로 유지한다.

v2.8에서는 사용자가 채택한 GCP 우선안을 실행 가능한 로컬 후보로 만들었다. e2-micro·미국 후보 리전·SSH /32·WireGuard UDP·무서비스계정·Shielded VM 계획, 프로젝트 밖 runtime config 생성, 무네트워크 설정검사, 동일 증거 해시의 첫 실제 노드 admission을 구현했다. 관련 안전 회귀 87/87 통과. `gcloud`와 계정은 아직 없고 실제 서버·기기 증거도 없으므로 전체 기준선은 7/12 = 58.3%다.

v2.9-r2에서는 v7.5 골든 #17·#18을 적용했다. `progress_dashboard.py`가 고정 기준선·가중치·증거 파일에서 실제 58.3%, 오늘 계획 58.3%, 일정 차이 0.0일, 남은 5개를 생성하며 설정·HTML 해시를 남긴다. 공개 앱은 HTTP 200·146,845 bytes·로컬 Pages 정본과 SHA-256 일치, Cloud Console과 Cloud Shell은 로그인으로 302, 로컬 gcloud는 불응답, 실제 VM은 0대로 조건별 차단을 분리했다. 문서나 대시보드 생성만으로 마일스톤 완료율은 오르지 않는다.

v2.10에서는 로컬 gcloud 불응답을 임계 경로에서 제거하기 위해 Cloud Shell 묶음 생성기를 추가했다. 읽기 전용 `01_preflight.sh`, 비용·project·APPLY 3중 게이트의 `02_deploy.sh`, admission/R6를 거짓으로 올리지 않는 `03_provider_readback.sh`, 정확한 VM·예약 IP·방화벽 2개만 지우는 `04_rollback.sh`를 plan/cloud-init 해시에 결속한다. EXAMPLE_ONLY는 모든 cloud 명령 전에 종료하고 관련 안전검사 7/7을 통과했다. 실제 VM 0대이므로 전체 마일스톤은 오르지 않는다.

v2.12에서는 GCP 공개 공식 가격을 로컬 계산 계약으로 고정했다. 무료 등급 e2-micro 후보라도 외부 IPv4는 월 730시간 기준 USD 3.65이며, 한국 목적지 Premium Tier 송신은 무료 허용량 뒤 GiB당 USD 0.19로 계산한다. `run_gcp_cost_review.py`는 1/10/100GiB 비용 하한을 생성하고, 로그인된 결제 콘솔에서 확인하지 못한 VM·디스크·크레딧·통화 조건은 UNKNOWN으로 남긴다. 이는 비용 검토 준비(I/L)이며 실제 결제·VM·서버 증거는 아니므로 전체 기준선은 7/12 = 58.3%다.

v2.13에서는 S-1 비용 확인을 로컬 단일 HTML 도우미로 만들었다. 사용량별 비용 하한·계정별 공개 확인값·6개 생성 전 체크를 한 화면에 모으고, 저장 실패 시 인메모리 유지와 JSON 내보내기/가져오기로 데이터 손실·기능 정지를 막는다. 계약 7/7·JavaScript 구문 1/1·localhost HTTP 200은 통과했지만 브라우저 창 연결 실패로 실제 상호작용은 미검증이다. 실제 결제·VM·서버 증거가 아니므로 전체 기준선은 7/12 = 58.3%다.

v2.14에서는 사용자 승인에 따라 S-1을 직접 실행했다. 앱 내 브라우저와 Chrome은 Google Cloud 페이지 제어가 시간 초과됐고, Windows 화면 제어는 창 조회 단계에서 시간 초과됐으며, 로컬 `gcloud`는 설치되지 않았다. 계정 상태를 추정하지 않고 `S1_BLOCKED_EXECUTION_TRANSPORT`로 기록했다. 과금 리소스 생성은 0건이고 실제 결제·VM·서버 증거도 0이므로 전체 기준선은 7/12 = 58.3%를 유지한다.

v2.15-r2에서는 v7.6의 상시 QR·당초/현재 완료일 분리·실측 전망선·3개 출시 로드맵·4요소 병목 경보·쉬운 용어 원장을 진척 대시보드에 구현했다. 공개 앱 QR은 저장된 디코더 증거와 이미지 SHA-256을 다시 결속하며, 최근 2026-08-02~03 완료 증가가 0%/일이면 현재 예상일을 지어내지 않는다. 동시에 기존 공개 소개 화면에 `앱 열기 → 내 기기 설정 받기 → 연결·보호 확인` 3단계를 추가하고, 현재는 1단계 화면 체험만 가능하다고 고정했다. 대시보드 7/7·GitHub Pages 23/23·전체 회귀 453/453·데스크톱/모바일 가로 넘침 0을 통과했다. 공개 저장소 후보는 로컬 미커밋·미배포이며 공개 앱 v2.3은 그대로다. 보고·안내 개선은 실제 서버·기기 증거가 아니므로 전체 기준선은 7/12 = 58.3%를 유지한다.

같은 v2.15 준비 묶음에서 이용약관·개인정보 처리방침·개인정보 국외이전 고지 초안 팩 v0.1을 만들었다. 2026-08-03 기준 국가법령정보센터의 개인정보 보호법 제30조·제28조의8, 시행령 제29조의10·제31조와 개인정보보호위원회 2025.4 작성지침을 출처로 연결했다. 실제 코드에서 확인된 현재 공개본의 무API·메모리/브라우저 저장 경계와 미래 가명 ID·공개키·사용량 원장을 구분했다. 미확정 35곳과 공개 금지 문구·P0 점검표를 남겼으므로 이는 법률 검토·동의 수집·공개 준비 완료가 아니다. 실제 사업자·공급자·국가·보존기간·환불·아동 정책과 전문가 검토 전까지 차단한다.

## 2. 실제 외부 권한이 필요한 것

- **대행동: VPN 서버 결제·계정·SSH 접근**
- **대행동: 실제 PG 계약·비밀값·환불 권한**
- **대행동: 앱스토어 계정과 심사**
- **소행동: 목표 실기기 연결·조작**
- **대행동: 파일럿 모집·동의·연락**
- **대행동: 법률·세무 자문과 호스터 서면 확인**

## 3. 출시 전 P0 관문

- 대한민국·서울 서버 정책 충돌 제거
- 실제 보호 안 됐는데 보호됨 표시하는 경로 0
- iOS·Android·Windows에서 DNS·IPv6·킬 스위치·재연결 전건 통과
- 잔액 음수·중복 차감·중복 추천 지급 0
- 개인정보·개인키 유출 0
- 실제 결제 전 가격이 설계안임을 명확히 표시

## 4. 이번 업그레이드에서 후행할 것

- 100GB·300GB 기본 판매
- 20개국 동시 확장
- 네이티브 앱 선제 개발
- 출석·레벨·배지
- 스트리밍 해제·검열 우회 홍보

## 5. 다음 공개 릴리스

다음 사용자 가치 릴리스는 **R6 실제 VPN 안전 검증**이다. 서버 접근 전에는 제어 API와 OS 안전 하네스를 로컬에서 준비하되, 실제 터널·출구 IP·DNS/IPv6·재연결 증거가 없으면 공개 화면의 `limited` 상태를 `protected`로 바꾸지 않는다.

## 6. 알파 최속 목표

- 첫 가시 변화: 구현 시작 후 4~6 개발시간
- A0 무료 알파 후보: 44~60 개발시간
- A0 외부 알파: 서버 접근 후 최단 5개 작업일
- 실제 결제: A1 유료 알파로 분리

시간은 계획 추정치이며 서버·기기·참여자 대기는 별도다. 검사를 생략해 맞춘 시간은 무효다.
